# Local config (may contain API keys later) — never commit.
.env

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/

# Build artifacts
*.egg-info/
build/
dist/

# Agent workspace is a sandbox, not source.
workspace/
